language: C
sudo: false
compiler:
  - gcc
  - clang
script:
  - make
  - make tests
addons:
  apt:
    packages:
      - liblua5.2-dev
  homebrew:
    packages:
      - lua
    update: true
os:
  - linux
  - osx
